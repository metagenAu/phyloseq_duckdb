% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_data_duckdb.R
\name{sample_data_duckdb}
\alias{sample_data_duckdb}
\title{Create a DuckDB-based sample data table}
\usage{
sample_data_duckdb(object, db_path = tempfile())
}
\arguments{
\item{object}{A data.frame or existing sample_data object}

\item{db_path}{Path to store the DuckDB database. Default is temporary}
}
\value{
A DuckDB connection with sample data table
}
\description{
This function creates and manages sample metadata using DuckDB for efficient storage
and querying of sample information. The data is stored in a columnar format
optimized for fast filtering and joining operations with OTU tables.
}
\examples{
# Create from data frame
sample_df <- data.frame(
  sample_id = paste0("Sample", 1:5),
  Treatment = c("Control", "Treatment", "Control", 
                "Treatment", "Control"),
  Time = c(0, 0, 24, 24, 48)
)
sdata <- sample_data_duckdb(sample_df)

# Get unique treatments
treatments <- unique_sample_values_duckdb(sdata, "Treatment")

# Don't forget to close the connection
close_sample_data_duckdb(sdata)
}
\seealso{
\code{\link{filter_sample_data_duckdb}}, \code{\link{unique_sample_values_duckdb}}
}
